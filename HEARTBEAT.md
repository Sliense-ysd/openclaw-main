# HEARTBEAT.md - Main Workspace Checklist

- 检查今天的 `memory/YYYY-MM-DD.md` 是否存在；如不存在，可创建空白日记文件。
- 检查是否有 workspace 的 `memory/` 根目录文件数长期超过 20；如有，在 `memory/heartbeat-log.md` 记录清理建议。
- 仅在发现新的稳定规则、关键决策、长期事实时，才建议更新 `MEMORY.md`；禁止把长报告或执行流水写入 `MEMORY.md`。
