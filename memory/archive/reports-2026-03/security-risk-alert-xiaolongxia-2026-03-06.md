# 安全风险分析报告 - 小龙虾事件

## 时间
2026-03-06 13:11 GMT+8

## 事件背景

从 X (Twitter) 信息来源：`@tuzi_ai` 发推称「用小龙虾导致浏览器中的信用卡信息被泄露，今日信用卡已嘎嘎」。

引用了一条推文（来自 `@realNyarime`），显示 OpenClaw 在某次操作中出现了严重的安全问题：「OpenClaw：「找到了。操他妈的。」」

根据推文内容和截图，这个问题可能与：
1. 浏览器自动填充的信用卡信息被恶意网站获取
2. OpenClaw 在浏览器自动化过程中意外暴露了敏感信息

## 我们的风险评估

### 当前配置检查

**配置文件权限**：
- `~/.openclaw/openclaw.json` - 包含多个 API Keys
- 已检测到以下 LLM Provider API Keys（脱敏显示）：
  - codesome: `sk-076***3b03`
  - kimi: `sk-kim***M7we`
  - kimi (另一个): `sk-5h1***hRKx`
  - friend: `sk-9ce***abca`
  - zhipu: `1470***svjb` (DashScope 格式)
  - friend-codesome: `cr-56a***8f01`
  - Coding Plan: `sk-sp-dd***7619`
  - xai: `BSAe***_7dt`

**潜在风险点**：

1. **浏览器自动化风险** ⚠️ 中等风险
   - OpenClaw 配置了浏览器中继（端口 18790）
   - 浏览器配置了 SSRF allowPrivateNetwork
   - 如果自动化脚本访问恶意网站，可能触发浏览器自动填充或 Cookie 泄露

2. **配置文件权限** ⚠️ 中等风险
   - API Keys 明文存储在 `openclaw.json` 中
   - 如果子 agent 有文件访问权限，可能读取到这些 keys
   - 虽然有 Memory 记录中的安全规则，但需要持续验证

3. **子 Agent 隔离** ⚠️ 低风险（已加固）
   - 2026-02-27 已配置 `sandbox.mode = non-main`
   - 子 agent 默认不能读写全局配置
   - 需要持续验证这个隔离是否有效

4. **X.com 访问链路** ✅ 已加固
   - 已配置两级回退：`xai search-x.js` → `r.jina.ai`
   - 避免直接访问 Twitter，减少钓鱼风险

## 建议措施

### 立即执行（P0）

1. **验证浏览器配置**
   ```bash
   # 检查浏览器自动填充设置
   # 确保在自动化环境中禁用保存密码/信用卡
   ```

2. **审查所有浏览器自动化脚本**
   - 搜索所有使用 `browser` tool 的代码
   - 确保没有访问可疑网站或输入敏感信息

3. **验证子 Agent 隔离**
   ```bash
   # 测试子 agent 是否能读取 openclaw.json
   # 应该返回权限错误
   ```

### 短期加固（P1）

1. **API Key 轮换**
   - 按照 2026-02-27 的 P0 清单执行密钥轮换
   - 优先轮换暴露风险高的 keys

2. **浏览器安全策略**
   - 考虑为自动化浏览器创建专用 profile
   - 禁用自动填充、密码保存功能
   - 启用更严格的 SSRF 策略

3. **定期安全审计**
   - 每周扫描配置文件中的敏感信息
   - 验证权限配置是否有效

### 长期方案（P2）

1. **密钥管理服务**
   - 考虑使用专门的密钥管理工具
   - 避免在配置文件中明文存储

2. **浏览器沙箱**
   - 使用容器化的浏览器环境
   - 隔离自动化操作和用户数据

3. **安全监控**
   - 实时监控异常访问
   - 检测敏感信息的异常传输

## 结论

**当前风险评估：中等风险**

虽然我们已经有一些安全措施（sandbox 隔离、x.com 回退等），但"小龙虾"事件提醒我们：

1. 浏览器自动化是最容易暴露敏感信息的环节
2. 配置文件明文存储是长期隐患
3. 需要定期验证安全措施是否有效

**下一步行动**：
1. 验证当前浏览器自动化脚本的安全性
2. 执行 API Key 轮换（按照 P0 清单）
3. 更新 MEMORY.md，记录这次风险分析

---

**记录人**: 主Agent
**状态**: 待执行
**优先级**: P0
