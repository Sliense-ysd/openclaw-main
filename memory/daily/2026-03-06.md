# 2026-03-06 Daily Notes

## Agent Permission Opening
- **13:14 CST**: Opened agent exec/elevated permissions globally per user request.
  - 原因: 用户明确要求“不是限制权限，全部都开放”，不再只给主 agent 或单个 Telegram 用户。
  - 具体细节:
    - `agents.defaults.elevatedDefault = "full"`
    - `tools.elevated.allowFrom.telegram = ["*"]`
    - `tools.exec.security = "full"`
    - `tools.exec.ask = "off"`
    - 删除各 agent 上额外的 `tools.elevated` 限制块，避免只对主 agent 放权
  - 生效验证:
    - `openclaw config validate` 通过
    - `openclaw gateway restart` 完成
    - `openclaw gateway status` 显示 `RPC probe: ok`

## Coding Agent Routing Attempt
- **2026-03-06**: Grayed `coding` agent to prefer Codex and Claude Code.
  - 原因: 用户要求先让开发助手尝试直接使用 Codex 和 Claude Code，其他 agent 保持不变。
  - 具体细节:
    - `coding` 模型链路改为 `codex-cli/gpt-5.3-codex -> claude-cli/opus-4.6 -> zhipu-anthropic/claude-sonnet-4-5-20251022`
    - 增加 `cliBackends.codex-cli` 与 `cliBackends.claude-cli`
    - `codex-cli` 采用 `workspace-write` 与 `--ask-for-approval never`
  - 当前状态:
    - 路由配置已生效
    - live probe 显示 `codex-cli` / `claude-cli` 仍是 missing
    - `openai-codex` OAuth probe 报 `invalidated`
    - 实测 `coding` agent 仍可返回，但当前实际回退到 `zhipu-anthropic`

## Agent Availability Validation
- **13:15-13:18 CST**: Probed non-main agents with `openclaw agent`.
  - 正常返回: `research`, `product`, `growth`, `operations`, `backlink`, `task`, `kol`, `users`
  - `coding`: 可返回，但探活提示被自身工作流上下文覆盖，实际 provider/model 为 `zhipu-anthropic/claude-sonnet-4-5-20251022`
  - 结论: 其余 agent 当前可用；`coding` 当前“可用但未真正跑在 Codex/Claude CLI 链路上”
