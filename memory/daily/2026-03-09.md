# 2026-03-09

## 文件存放规范更新

- 主人明确要求：项目开发统一放在 `/Users/openclaw/workspace/<project>/`。
- 不要在 `/tmp/` 下 clone 项目，避免重复下载和临时目录漂移。
- Git 操作统一在项目目录中执行：`git pull`、`git add`、`git commit`、`git push`。
- Coding Agent 使用时显式指定 `workdir:/Users/openclaw/workspace/<project>`。

## 已清理目录

- `/Users/openclaw/workspaces`
- `/tmp/nilaio.com`
- `workspace/auto-extern-link`
- `workspace/deployment-investigation`

## “龙虾”使用原则

- 主人补充了自动化边界：只有高频/高重复、明显耗时、且 AI 可稳定交付的任务，才值得交给“龙虾”。
- 低频、低耗时、或 AI 质量暂时不够的任务，先人工处理。
- 后续我在提出自动化建议前，要先判断这件事的复利是否足够强。

## 主 Agent 回复格式调整

- 主人明确要求：主 Agent 默认不要播报“我先按工作区规则/先读上下文/先按输出规范”等内部动作。
- Telegram 风格改为一句一句回：一行一个重点，结论、证据、下一步分开写。
- 除非主人明确要长文，否则避免把多个重点揉成一整段。
